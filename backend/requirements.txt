# CobraQ Backend
# Chạy: cd backend && uvicorn app.main:app --reload --port 8000

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pymupdf>=1.24.0
python-docx>=1.1.0
# AI Provider (Grok xAI / OpenAI / OpenAI-compatible — unified via openai SDK)
openai>=1.50.0

# Legacy Anthropic SDK (optional, kept for vision parsing of scanned PDFs)
anthropic>=0.25.0

# Bảo mật
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt<4.1
pydantic-settings>=2.0.0
email-validator>=2.1.0  # Required for EmailStr in auth.py (RegisterBody/LoginBody)

# Database (SQLAlchemy + SQLite + Alembic migrations)
SQLAlchemy>=2.0.0
alembic>=1.13.0

# Sanitization
bleach>=6.1.0

# PDF image conversion (cho Claude Vision)
pdf2image>=1.17.0
Pillow>=10.0.0

# Vector DB
chromadb>=0.5.0

# Token counting (context window)
tiktoken>=0.7.0

# Rate limiting
slowapi>=0.1.9

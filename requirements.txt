fastapi
uvicorn
python-multipart
pydantic
pymupdf
python-docx
google-generativeai
mysql-connector-python
python-dotenv
bcrypt
PyJWT
pix2tex
Pillow

# === AI Pipeline Dependencies ===
sentence-transformers>=2.2.0    # Embedding model (multilingual)
chromadb>=0.4.18                # Vector database
scikit-learn>=1.3.0            # Cosine similarity utilities
numpy>=1.24.0                  # Already used, but ensure version

# Optional: Better embeddings (uncomment if needed)
# openai>=1.0.0                # OpenAI embeddings (higher quality)
# cohere>=4.0.0                # Cohere embeddings

# Optional: Rate limiting & async
# slowapi>=0.1.8              # Rate limiting for API
# celery>=5.0.0               # Background tasks (async processing)
# redis>=4.5.0                # Celery broker

# Optional: Token counting (cost estimation)
# tiktoken>=0.5.0             # OpenAI tokenizer (if using OpenAI)
